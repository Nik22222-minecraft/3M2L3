🧠 Telegram Бот «Бот-портфолио»

Кратко: бот хранит информацию о твоих проектах, их статусе и используемых навыках. Данные — в SQLite (файл portfolio.db).

О ПРОЕКТЕ:

Этот проект — учебный Telegram-бот,
который помогает сохранять проекты,
добавлять к ним навыки, менять статус,
удалять проекты и просматривать информацию. 
Проект реализован на Python + pyTelegramBotAPI и использует SQLite.


СТРУКТУРА ПРОЕКТА:
bot_portfolio/
│
├── config.py            # TOKEN, DATABASE
├── logic.py             # DB_Manager — логика работы с БД
├── main.py              # примерный тестовый запуск (тесты / быстрый пример)
├── main2.py             # основной TeleBot (интерактивный бот) — добавлен
├── alter_projects.py    # скрипт для ALTER TABLE (добавление photo)
├── clear_projects.py    # (опц.) скрипт для очистки таблицы projects
├── README.md            # этот файл
└── portfolio.db         # БД (создаётся после первого запуска)
Примечание: main2.py — это рабочий файл бота с хэндлерами, клавиатурами, командами /new_project, /projects, /skills, /delete, /update_projects, /info и т.д.



Как запустить :
Установи зависимости:

pip install pyTelegramBotAPI


Вставь токен в config.py:

TOKEN = "тут_твой_токен"
DATABASE = "portfolio.db"


Создай таблицы и добавь дефолтные данные (один вариант — запустить main.py или импортировать и вызвать):

python main.py


Запустить бота (основной файл):

python main2.py

Что делает main2.py (кратко)

Инициализирует TeleBot и DB_Manager.

Реализованы команды:

/start — приветствие;

/info — подробная справка по всем командам (доработано);

/new_project — добавление проекта (одноразовая reply-клавиатура для статусов);

/projects — список проектов с inline-кнопками;

/skills — добавление навыков к проекту;

/delete — удаление проекта;

/update_projects — обновление полей проекта (имя, описание, ссылка, статус);

Reply-клавиатуры сделаны одноразовыми (one_time_keyboard=True) — они скрываются после выбора.

Красивое форматирование сообщений, эмодзи, комментарии для каждого хэндлера (документирование).



РЕАЛИЗОВАННЫЕ SQL-ЗАПРОСЫ И МЕТОДЫ (коротко)

В logic.py реализованы:

create_tables() — создание таблиц (projects, skills, status, project_skills) с IF NOT EXISTS.

default_insert() — добавление дефолтных навыков и статусов (без дубликатов).

insert_project(data) — вставка проекта(ов).

insert_skill(user_id, project_name, skill) — связать навык с проектом.

get_statuses(), get_status_id(status_name) — работа со статусами.

get_projects(user_id), get_project_id(...) — получение проектов.

get_skills(), get_project_skills(project_name), get_project_info(user_id, project_name) — просмотр.

update_projects(...), delete_project(...), delete_skill(...) — редактирование и удаление.

Доп. методы: add_skill, update_skill, delete_status, update_project_status.

ЗАДАНИЯ , КОТОРЫЕ УЖЕ РЕАЛИЗОВАННЫ

 Создание таблиц и типы полей

 Добавление дефолтных статусов и навыков (без дубликатов)

 Добавление столбца photo через alter_projects.py (ALTER TABLE)

 One-time reply клавиатуры (one_time_keyboard=True) в main2.py

 Подробная /info с описанием команд

 Комментарии/документирование хэндлеров в main2.py

 Интерфейс: эмодзи, приятный вывод проектов + inline кнопки


 Документирование (кратко что сделано)

Оставил комментарии в main2.py над каждым хэндлером.

В logic.py — комментарии для основных методов.

При желании: добавить docstrings (строки-описания) в каждую функцию logic.py и main2.py.
